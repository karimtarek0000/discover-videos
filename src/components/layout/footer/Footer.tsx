const Footer = (): JSX.Element => {
  return (
    <footer className="py-2 text-center text-gray-500">
      <p>&copy; {new Date().getFullYear()} - karim tarek</p>
    </footer>
  );
};

export default Footer;
